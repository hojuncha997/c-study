#include <stdio.h>

int main(void)
{
    int kor = 70;
    int eng = 80;
    int mat = 90;

    int tot = kor + eng + mat;
    printf("%s %d%s\n", "총점은", tot, "입니다.");
    //  총점은 240입니다.
    return 0;
}